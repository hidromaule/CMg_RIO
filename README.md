Para compilar (empaquetar) nuevo ejecutable:

1. Descargar todos los archivos del repositorio.
2. En el terminal de python, ejecutar el siguiente comando en la ruta de los archivos: pyinstaller run.spec
3. Una vez compilado, el ejecutable queda en la carpeta /dist
4. Copiar el código CMg_RIO.py en la carpeta /dist
5. Ejecutar el .exe
